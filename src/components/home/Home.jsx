import React from 'react'
import "./Home.css"

const Home = () => {
  return (
    <div>
        <button onClick={null}>Login</button>
        <button onClick={null}>Play as a guest</button>
    </div>
  )
}

export default Home