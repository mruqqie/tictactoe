import React from "react";
import "./PwComputer.css"

const PwComputer = () => {
	return (
		<div className="pwComputer">
			<button className="button" onClick={null}>
				Play With Computer
			</button>
		</div>
	);
};

export default PwComputer;
